[Unit]
Description=Quest Esports encrypted production backup
After=network-online.target
Wants=network-online.target
OnFailure=quest-esports-backup-failure@%n.service

[Service]
Type=oneshot
User=deploy
Group=deploy
# /etc/quest-esports-backup is root:deploy 0750 and
# backup-client-ca.crt plus backup-client.{crt,key} are root:deploy 0640;
# deploy is the only non-root identity permitted to traverse/read the client
# TLS material. Server TLS remains under /etc/quest-esports/tls for the UID999
# PostgreSQL container contract.
Environment=BACKUP_ENV_FILE=/etc/quest-esports-backup.env
WorkingDirectory=/var/www/QuestEsports
ExecStart=/usr/bin/bash /var/www/QuestEsports/ops/backup-production.sh
PrivateTmp=true
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/srv/quest-esports /var/lock/quest-esports-release.lock

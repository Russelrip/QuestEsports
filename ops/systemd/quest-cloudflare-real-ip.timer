[Unit]
Description=Weekly refresh of the Cloudflare real-IP ranges trusted by Nginx

[Timer]
OnCalendar=Sun 04:30
RandomizedDelaySec=30m
# Cloudflare range changes are rare but the file must not silently rot if the
# host was off when the timer was due.
Persistent=true

[Install]
WantedBy=timers.target
